<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <!-- Base on a recent, stable Spring Boot 2.x version -->
        <version>2.5.0</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <groupId>com.salescode.dis</groupId>
    <artifactId>insights-sdk</artifactId>
    <version>0.0.8-SNAPSHOT</version>
    <name>insights-sdk</name>
    <description>Insights SDK for interacting with the insights service</description>

    <properties>
        <java.version>11</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <lombok.version>1.18.30</lombok.version>
        <insights-common.version>0.0.8-SNAPSHOT</insights-common.version>
    </properties>

    <repositories>
        <repository>
            <id>salescode-dis-insights</id>
            <name>salescode-dis-insights</name>
            <url>https://salescode-008136251604.d.codeartifact.ap-south-1.amazonaws.com/maven/dis-insights/</url>
        </repository>
    </repositories>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>com.salescode.dis</groupId>
            <artifactId>insights-common</artifactId>
            <version>${insights-common.version}</version>
            <scope>compile</scope>
            <!-- Classifier will be handled by profile for sb3 version -->
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>3.4.1</version> <!-- Use a recent version -->
                <executions>
                    <execution>
                        <id>enforce-java</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <requireJavaVersion>
                                    <!-- Enforce JDK 11 or newer for Maven execution -->
                                    <version>[11,)</version>
                                </requireJavaVersion>
                            </rules>
                            <fail>true</fail> <!-- Fail the build if rules are not met -->
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <!-- Version inherited from Spring Boot parent (2.7.18) -->
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>

            <!-- Add Maven Shade Plugin for creating Uber JAR -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.5.0</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                            <filters>
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                            <!-- Keep the original JAR as the main artifact -->
                            <shadedArtifactAttached>false</shadedArtifactAttached>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>sb3</id>
            <activation>
                <property>
                    <name>sb3</name>
                </property>
            </activation>
            <properties>
                <artifact.classifier>sb3</artifact.classifier>
                <transformed.output.directory.sdk>${project.build.directory}</transformed.output.directory.sdk>
                <!-- This property tells the SDK to depend on the jakarta-classified common lib -->
                <insights-common.classifier>sb3</insights-common.classifier>
            </properties>

            <dependencies>
                <!-- Depend on the Jakarta-transformed common module when this profile is active -->
                <dependency>
                    <groupId>com.salescode.dis</groupId>
                    <artifactId>insights-common</artifactId>
                    <version>${insights-common.version}</version>
                    <classifier>${insights-common.classifier}</classifier>
                </dependency>
            </dependencies>

            <build>
                <plugins>
                    <!-- Attaches the JAR with the '-sb3' classifier to the project, making it available for installation/deployment. -->
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>build-helper-maven-plugin</artifactId>
                        <version>3.5.0</version>
                        <executions>
                            <execution>
                                <id>attach-jakarta-sdk-artifact</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>attach-artifact</goal>
                                </goals>
                                <configuration>
                                    <artifacts>
                                        <artifact>
                                            <file>${project.build.directory}/${project.build.finalName}.jar</file>
                                            <type>jar</type>
                                            <classifier>${artifact.classifier}</classifier>
                                        </artifact>
                                    </artifacts>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.eclipse.transformer</groupId>
                        <artifactId>transformer-maven-plugin</artifactId>
                        <version>0.5.0</version>
                        <extensions>true</extensions>
                        <executions>
                            <execution>
                                <id>jakarta-transform-common</id>
                                <goals>
                                    <goal>jar</goal>
                                </goals>
                                <configuration>
                                    <artifact>
                                        <groupId>${project.groupId}</groupId>
                                        <artifactId>${project.artifactId}</artifactId>
                                        <version>${project.version}</version>
                                        <classifier>${artifact.classifier}</classifier>
                                    </artifact>
                                </configuration>
                            </execution>
                        </executions>
                        <configuration>
                            <rules>
                                <jakartaDefaults>true</jakartaDefaults>
                            </rules>
                        </configuration>
                        <dependencies>
                            <dependency>
                                <groupId>org.eclipse.transformer</groupId>
                                <artifactId>org.eclipse.transformer.jakarta</artifactId>
                                <version>0.5.0</version>
                            </dependency>
                        </dependencies>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

    <distributionManagement>
        <repository>
            <id>salescode-dis-insights</id>
            <name>salescode-dis-insights</name>
            <url>https://salescode-008136251604.d.codeartifact.ap-south-1.amazonaws.com/maven/dis-insights/</url>
        </repository>
    </distributionManagement>

</project>